Job would satisfy no-touch specs, except contains multiple different
board images.  This is for a single small instrument which for
packaging reasons is split into 5 boards.  For prototype I can cut the
sub-boards myself, so only need a single rectangle routed.

top silk: HANDLE-RESOLVE.TSK

top mask: HANDLE-RESOLVE.TMS

top signal layer: HANDLE-RESOLVE.TOP

ground plane layer (negative): HANDLE-RESOLVE.GND

inner signal layer: HANDLE-RESOLVE.IN

bottom signal layer: HANDLE-RESOLVE.BOT

board outline and drill symbols: HANDLE-RESOLVE.BRD

NC drill: HANDLE-RESOLVE.DRL
